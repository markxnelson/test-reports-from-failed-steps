box: ubuntu

build:
  steps:
    - script:
      name: good one
      code: |
        touch $WERCKER_REPORT_ARTIFACTS_DIR/good
    - script:
      name: bad one
      code: |
        touch $WERCKER_REPORT_ARTIFACTS_DIR/bad
        exit

